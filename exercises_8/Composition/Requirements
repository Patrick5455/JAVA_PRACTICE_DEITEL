DATE CLASS REQUIREMENT
- Constructor
    check if month in range
    check if day in range for month
    check for leap year if month is 2 and day is 29
- toString method to return a string of the form month/day/year


EMPLOYEE CLASS REQUIREMENT
- Attribute
    firstName
    lastName
    hireDate
    birthDate

-Methods
    Constructor
        set all attributes as parameters
    toString method
        return string format of constructor parameters