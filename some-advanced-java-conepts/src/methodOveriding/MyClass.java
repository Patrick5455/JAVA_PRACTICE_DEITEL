package methodOveriding;

public class MyClass {

    /* when a derived class methods overrides the method of the base class
    *it occurs in polymorphism and interface
     *rules;
     * the method overiding must conatins the same return type and
     * number of parameters as the overrided methos
     *
     * Method overloading is different form overridnig in that in method
     * overloading, the overloaded method(s) are in the same class
     * Also the overloaded method can contain a different return type and
     * nmber of parameters with the inital method
     * they only share similarities in method name
     */

}
